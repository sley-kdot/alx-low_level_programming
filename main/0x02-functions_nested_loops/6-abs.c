#include "main.h"
#include <stdlib.h>
/**
 * _abs - function that computes the value of an integer
 * @num: single digit input
 * @num_abs
 * Return: num (success)
 */
int _abs(int num)
{
	int num_abs;

	num_abs = abs(num);
	return (num_abs);
}
